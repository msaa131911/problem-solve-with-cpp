/*একজন ম্যানেজারকে একটি প্রজেক্ট শেষ করতে N সপ্তাহ সময় দেওয়া হয়
এবং প্রত্যেক সপ্তাহে M দিন করে কাজ করতে পারে ।*/

#include <iostream>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    cout << (n + 1) * m;//formula → (N + 1) × M

    return 0;
}
